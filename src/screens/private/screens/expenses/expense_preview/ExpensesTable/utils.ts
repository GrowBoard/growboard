// export const getExpenseResponse = (expense: ExpenseSqlResponse[]) => {
//   return Object.values(Category).reduce(
//     (acc, category) => {
//       acc[category] =
//         expense.find((exp) => exp.category === category)?.amount || 0;
//       return acc;
//     },
//     {} as Record<Category, number>,
//   );
// };

export const a = 1;
