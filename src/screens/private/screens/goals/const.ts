/**
 * Maximum character limit for goal title.
 */
export const MAX_TITLE_LENGTH = 100;

/**
 * Maximum number of tags allowed for a single goal.
 */
export const MAX_TAGS = 10;

/**
 * Maximum character limit for a tag.
 */
export const MAX_TAG_LENGTH = 20;

/**
 * Maximum number of timeline entries allowed for a single goal.
 */
export const MAX_TIMELINE_ENTRIES = 20;

/**
 * Maximum character limit for a timeline entry.
 */
export const MAX_TIMELINE_ENTRY_LENGTH = 150;
